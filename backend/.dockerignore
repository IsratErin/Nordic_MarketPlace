# Ignore node_modules (will be installed in Docker)
node_modules

# Ignore environment files (should be injected at runtime)
.env
.env.*
!.env.example

# Ignore test and coverage files
coverage
*.log
logs
tests
*.test.ts
*.spec.ts

# Ignore version control and editor files
.git
.gitignore
.vscode
.idea

# Ignore OS and build artifacts
npm-debug.log
.DS_Store
*.swp

# Notes:
# - .env files are ignored for security; use Docker secrets or pass env vars at runtime.
# - Only .env.example is included for reference.
# - Exclude test and coverage files to keep the image small and secure.